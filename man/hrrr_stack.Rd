% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hrrr-smoke.R
\name{hrrr_stack}
\alias{hrrr_stack}
\title{Load preprocessed HRRR-smoke variables into a stacked SpatRaster}
\usage{
hrrr_stack(
  dt,
  input_path,
  variables = c("APCP", "MASSDEN", "TMP", "VGRD", "UGRD", "RH", "HPBL")
)
}
\arguments{
\item{dt}{A \code{Date} or date-coercible value specifying the day to load.}

\item{input_path}{Directory containing preprocessed HRRR GeoTIFF files (output
of \code{\link{hrrr_acquire}}).}

\item{variables}{Character vector of variable names to load. Files are expected
to follow the naming convention \code{VARNAME_YYYYMMDD_hrrrsmoke.tif}.
Defaults to \code{c("APCP", "MASSDEN", "TMP", "VGRD", "UGRD", "RH", "HPBL")}.}
}
\value{
A \code{SpatRaster} with one layer per variable, named by
  \code{variables}.
}
\description{
Reads the per-variable GeoTIFF files written by \code{\link{hrrr_acquire}} for a
given date and stacks them into a single multi-layer \code{SpatRaster}, with each
layer named by its variable.
}
\examples{
\dontrun{
hrrr_stack("2024-11-15", input_path = "./processed_data/HRRR/")
}
}
