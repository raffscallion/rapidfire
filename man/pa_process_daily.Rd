% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/purpleair.R
\name{pa_process_daily}
\alias{pa_process_daily}
\title{Process daily PurpleAir sensor history into a spatial dataset}
\usage{
pa_process_daily(
  pa_raw,
  pa_sensors,
  timezone = "America/Los_Angeles",
  clean_outliers = TRUE,
  crs = "EPSG:3395",
  output_path = "./processed_data/purpleair/"
)
}
\arguments{
\item{pa_raw}{A data frame of raw hourly PurpleAir data as returned by
\code{\link{pa_sensor_history_daily}}, containing \code{sensor_index},
\code{time_stamp}, and \code{pm2.5_atm} columns.}

\item{pa_sensors}{A data frame of sensor metadata as returned by
\code{\link{pa_find_sensors}}, containing \code{sensor_index}, \code{latitude},
and \code{longitude} columns.}

\item{timezone}{Local timezone string used to assign the correct calendar day.
Default is \code{"America/Los_Angeles"}.}

\item{clean_outliers}{Logical. If \code{TRUE}, apply spatial outlier removal via
\code{pa_clean_spatial_outliers}. Default is \code{TRUE}.}

\item{crs}{Coordinate reference system string passed to \code{terra::project}.
Default is \code{"EPSG:3395"}.}

\item{output_path}{Directory path where the processed RDS file will be saved.
Default is \code{"./processed_data/purpleair/"}.}
}
\value{
A \code{SpatVector} with daily mean PM2.5 (\code{PM25_daily}) and its
  log-transform (\code{PM25_log}), projected to \code{crs}. The object is also
  saved to \code{output_path} as \code{purpleair_processed_YYYY-MM-DD.RDS}.
}
\description{
Takes raw hourly sensor data from \code{\link{pa_sensor_history_daily}} and sensor
metadata from \code{\link{pa_find_sensors}}, computes daily mean PM2.5, log-transforms
it, and returns a projected SpatVector saved to disk. Only single-day inputs are
supported.
}
\seealso{
\code{\link{pa_sensor_history_daily}}, \code{\link{pa_find_sensors}}
}
