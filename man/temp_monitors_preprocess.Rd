% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temp_monitors.R
\name{temp_monitors_preprocess}
\alias{temp_monitors_preprocess}
\title{Convert an AirMonitor \code{mts_monitor} object to a projected SpatVector}
\usage{
temp_monitors_preprocess(mon, crs = "EPSG:3395", timezone = timezone)
}
\arguments{
\item{mon}{An \code{mts_monitor} object as returned by \pkg{AirMonitor} functions
such as \code{AirMonitor::monitor_combine}.}

\item{crs}{Coordinate reference system string passed to \code{terra::project}.
Default is \code{"EPSG:3395"}.}

\item{timezone}{Local timezone string used to assign hourly observations to the
correct calendar day.}
}
\value{
A \code{SpatVector} with columns \code{monitorID}, \code{Day},
  \code{PM25}, \code{Hours}, and \code{PM25_log}, or \code{NULL} if no monitors
  meet the data completeness threshold.
}
\description{
Pivots hourly monitor data to long format, joins site metadata, computes daily
mean PM2.5 in local time, and filters to days with at least 16 valid hourly
observations and positive mean values. Log-transforms the daily mean and returns
the result as a projected \code{SpatVector}.
}
\seealso{
\code{\link{temp_monitors_acquire}}
}
