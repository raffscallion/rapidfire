% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitors_terra.R
\name{monitors_krige_points}
\alias{monitors_krige_points}
\title{Krige PM2.5 values at point locations}
\usage{
monitors_krige_points(mon, locs, vgm, maxdist = Inf, nmax = Inf)
}
\arguments{
\item{mon}{A \code{SpatVector} of monitor observations with a \code{PM25_log}
column used as kriging input.}

\item{locs}{Prediction locations as a \code{SpatVector} or \code{sf} object.}

\item{vgm}{A \code{variogramModel} object (e.g., from
\code{\link{monitors_variogram}} or \code{\link{monitors_variogram_pooled}}).}

\item{maxdist}{Maximum search radius (in CRS map units) for the local kriging
neighborhood. Default is \code{Inf} (global kriging).}

\item{nmax}{Maximum number of nearest neighbors to include in kriging. Default
is \code{Inf}.}
}
\value{
An \code{sf} object at \code{locs} with columns \code{var1.pred}
  (kriged \code{PM25_log} predictions) and \code{var1.var} (prediction variance).
}
\description{
Performs ordinary kriging using a fitted variogram model to predict log-transformed
PM2.5 values at arbitrary point locations. Duplicate geometries are resolved by
taking the median \code{PM25_log} value, and non-finite values are removed before
kriging.
}
\seealso{
\code{\link{monitors_krige_grid}}, \code{\link{monitors_variogram}}
}
