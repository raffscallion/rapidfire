% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitors_terra.R
\name{monitors_krige_grid}
\alias{monitors_krige_grid}
\title{Krige PM2.5 values onto a prediction grid}
\usage{
monitors_krige_grid(mon, grid, vgm, maxdist = Inf, nmax = Inf)
}
\arguments{
\item{mon}{A \code{SpatVector} of monitor observations with a \code{PM25_log}
column used as kriging input.}

\item{grid}{A \code{SpatRaster} defining the target prediction grid (CRS, extent,
and resolution).}

\item{vgm}{A \code{variogramModel} object (e.g., from
\code{\link{monitors_variogram}} or \code{\link{monitors_variogram_pooled}}).}

\item{maxdist}{Maximum search radius (in CRS map units) for the local kriging
neighborhood. Default is \code{Inf} (global kriging).}

\item{nmax}{Maximum number of nearest neighbors to include in kriging. Default
is \code{Inf}.}
}
\value{
A \code{SpatRaster} with kriged \code{PM25_log} predictions interpolated
  onto \code{grid}.
}
\description{
Performs ordinary kriging using a fitted variogram model to predict log-transformed
PM2.5 values at every cell of a \code{SpatRaster} grid. Duplicate coordinates are
resolved by taking the median \code{PM25_log} value, and non-finite values are
removed before kriging.
}
\seealso{
\code{\link{monitors_krige_points}}, \code{\link{monitors_variogram}}
}
